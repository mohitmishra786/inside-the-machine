#!/bin/bash

chmod +x fix_chapter_frontmatter.rb
chmod +x cleanup_chapters.rb
chmod +x prepare_for_github.sh
chmod +x serve.sh
chmod +x fix_macos.sh
chmod +x create_chapter.rb
chmod +x create_chapter.sh

echo "All scripts are now executable!"